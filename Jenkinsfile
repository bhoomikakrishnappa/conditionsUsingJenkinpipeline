pipeline {
    agent any
    stages{
        stage(branch)
        {
            when {
                branch 'master'
            }
                echo 'your in master branch'
        }
    }    
}
