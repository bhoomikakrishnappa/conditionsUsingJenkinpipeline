pipeline {
    agent any
    stages{
        stage("Example")
        {
            when {
                branch "master"
            }
                echo 'your in master branch'
        }
    }    
}
